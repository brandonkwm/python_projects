pre-requisite: all input files placed in a folder in os.getcwd()
script will scan all files for certain string and output that string + the preceding lines (designated from user input e.g. 4 or 5 preceding lines)
script will output 2 files. 1 summary (stating the file name + line number where the string is found) + 1 with the preceding lines
